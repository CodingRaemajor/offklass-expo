// assets/transcripts/m3.ts

export const m3Transcript = `
Welcome to Multiplication Basics.

Today we will learn how multiplication works and how place value helps us multiply numbers.

Multiplication is a faster way of adding equal groups.

----------------------------------
MULTIPLICATION BY 1-DIGIT NUMBERS
----------------------------------

Multiplication means repeated addition.

Example:

3 × 4

This means 3 groups of 4.

4 + 4 + 4 = 12

So:
3 × 4 = 12

Another example:

5 × 2

2 + 2 + 2 + 2 + 2 = 10

So:
5 × 2 = 10

Multiplication helps us solve problems faster than repeated addition.

----------------------------------
COMPARING WITH MULTIPLICATION
----------------------------------

Multiplication helps us compare groups of numbers.

Example:

Sara has 3 boxes of pencils.
Each box has 5 pencils.

Instead of adding:
5 + 5 + 5

We multiply:

3 × 5 = 15

So Sara has 15 pencils.

Multiplication shows the total when groups are equal.

----------------------------------
MULTIPLICATION BY 10, 100, 1000
----------------------------------

Multiplying by 10, 100, or 1000 is easy using place value.

When we multiply by 10,
each digit moves one place to the left.

Example:

6 × 10 = 60  
23 × 10 = 230  

When we multiply by 100,
each digit moves two places to the left.

Example:

4 × 100 = 400  
35 × 100 = 3500  

When we multiply by 1000,
each digit moves three places to the left.

Example:

7 × 1000 = 7000  

Multiplying by powers of 10 adds zeros to the number.

----------------------------------
MULTI-DIGIT MULTIPLICATION (MODELS)
----------------------------------

Models help us understand multiplication visually.

One common model is the area model.

Example:

12 × 3

Break 12 into tens and ones:

12 = 10 + 2

Now multiply each part.

10 × 3 = 30  
2 × 3 = 6  

Add the results:

30 + 6 = 36

So:

12 × 3 = 36

Models help us see how multiplication works with place value.

----------------------------------
ESTIMATING PRODUCTS
----------------------------------

Estimating means finding a number that is close to the exact answer.

We round numbers to make multiplication easier.

Example:

49 × 6

49 is close to 50.

So we estimate:

50 × 6 = 300

The real answer will be close to 300.

Estimation helps us check if answers make sense.

----------------------------------
MULTIPLY WITH PARTIAL PRODUCTS
----------------------------------

Partial products break multiplication into smaller steps.

Example:

23 × 4

Break 23 into tens and ones.

23 = 20 + 3

Multiply each part.

20 × 4 = 80  
3 × 4 = 12  

Now add the partial products.

80 + 12 = 92

So:

23 × 4 = 92

Partial products make large multiplication easier to understand.

----------------------------------
REMEMBER
----------------------------------

• Multiplication means equal groups.
• It is a faster way than repeated addition.
• Place value helps when multiplying larger numbers.
• Estimating helps check if answers make sense.
• Partial products break multiplication into smaller steps.

Understanding multiplication helps us solve many real-world problems.
`;