// assets/transcripts/m5.ts

export const m5Transcript = `
Welcome to Division Basics.

Today we will learn how division works and how place value helps us divide numbers.

Division tells us how to split a number into equal groups.

----------------------------------
DIVISION
----------------------------------

Division means sharing or grouping equally.

Example:

12 ÷ 3

This means dividing 12 into 3 equal groups.

Each group will have:

4

So:

12 ÷ 3 = 4

Another example:

20 ÷ 5

20 split into 5 equal groups gives:

4 in each group.

----------------------------------
REMAINDERS
----------------------------------

Sometimes numbers cannot be divided equally.

The amount left over is called a remainder.

Example:

14 ÷ 3

3 groups of 3 equals 12.

But 2 is left over.

So the answer is:

14 ÷ 3 = 4 remainder 2

We write it as:

4 R2

----------------------------------
DIVIDE MULTIPLES OF 10, 100, 1000 BY 1-DIGIT NUMBERS
----------------------------------

Place value helps make division easier.

Example:

60 ÷ 3

60 is 6 tens.

6 tens ÷ 3 = 2 tens

2 tens = 20

So:

60 ÷ 3 = 20

Example:

800 ÷ 4

800 is 8 hundreds.

8 hundreds ÷ 4 = 2 hundreds

2 hundreds = 200

----------------------------------
DIVISION WITH PLACE VALUE
----------------------------------

We divide starting with the largest place value.

Example:

84 ÷ 2

Break 84 into:

80 + 4

Divide each part.

80 ÷ 2 = 40  
4 ÷ 2 = 2

Now add the results.

40 + 2 = 42

So:

84 ÷ 2 = 42

----------------------------------
DIVISION WITH AREA MODELS
----------------------------------

Area models can help us understand division.

Example:

96 ÷ 4

We break 96 into parts.

80 + 16

Divide each part.

80 ÷ 4 = 20  
16 ÷ 4 = 4

Add the results.

20 + 4 = 24

So:

96 ÷ 4 = 24

Area models help us see how numbers are split.

----------------------------------
ESTIMATE QUOTIENTS
----------------------------------

Estimating helps us find an answer close to the real one.

Example:

198 ÷ 4

198 is close to 200.

So estimate:

200 ÷ 4 = 50

The real answer will be close to 50.

Estimation helps us check our work.

----------------------------------
MULTI-DIGIT DIVISION WITH PARTIAL QUOTIENTS
----------------------------------

Partial quotients break division into smaller steps.

Example:

96 ÷ 4

First divide a large chunk.

80 ÷ 4 = 20

Now subtract.

96 − 80 = 16

Divide the rest.

16 ÷ 4 = 4

Add the partial quotients.

20 + 4 = 24

So:

96 ÷ 4 = 24

----------------------------------
MULTIPLICATION AND DIVISION WORD PROBLEMS
----------------------------------

Word problems help us apply multiplication and division.

Example:

There are 4 bags with 6 apples in each bag.

Total apples:

4 × 6 = 24

Example:

24 apples are shared equally among 4 students.

24 ÷ 4 = 6 apples each.

Multiplication and division are related.

----------------------------------
MULTI-STEP WORD PROBLEMS
----------------------------------

Some problems need more than one step.

Example:

A teacher has 30 pencils.

She puts them into boxes with 5 pencils in each box.

Step 1: Divide to find boxes.

30 ÷ 5 = 6 boxes

Step 2: If each box is given to 2 students, how many students get pencils?

6 × 2 = 12 students

Multi-step problems require solving one step at a time.

----------------------------------
REMEMBER
----------------------------------

• Division means splitting into equal groups.
• A remainder is what is left over.
• Place value helps divide larger numbers.
• Estimation helps check answers.
• Partial quotients break division into easier steps.

Understanding division helps solve many everyday problems.
`;